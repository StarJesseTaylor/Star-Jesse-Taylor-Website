/**
 * Star Jesse Taylor · Sticky announcement bar (DISABLED)
 *
 * The May 30, 2026 LA Intensive bar was removed after the workshop ran.
 * This file is kept as a no-op so the 21 pages that load it still find
 * the file (no 404s, no console errors). When the next event date is
 * locked, replace this no-op with the bar logic again, set EVENT_DATE
 * and EVENT_URL, and the bar returns on every page.
 */

(function () {
  'use strict';
  // No-op. Bar intentionally disabled.
})();
