/**
 * Star Jesse Taylor · Sticky announcement bar
 *
 * Retired 2026-07-05. Replaced by js/skool-banner.js which shows:
 *   "One free week of live calls with Star." + obvious "Try It Free" button
 *
 * The old countdown-to-launch bar is no longer needed (community launched
 * July 1, 2026). Kept as a no-op so pages that still reference this script
 * do not break their <script> tag, but nothing renders.
 */

(function () {
  'use strict';
  // Intentionally empty. See js/skool-banner.js for the current banner.
})();
